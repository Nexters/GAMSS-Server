-- 닉네임 길이 검증이 그래핌(사용자가 보는 글자) 기준으로 바뀐다(#129).
-- varchar(n)은 코드 포인트 기준이라, 그래핌 20자를 코드 포인트로 환산하면 훨씬 커질 수 있다
-- (예: ZWJ 가족 이모지 1자 = 코드 포인트 7개). 그래핌당 여유 있게 10개로 잡아 200으로 확장한다.
ALTER TABLE members MODIFY nickname VARCHAR(200) NULL;
