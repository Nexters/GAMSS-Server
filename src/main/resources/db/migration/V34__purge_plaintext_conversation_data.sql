-- 암호화 도입 이전에 쌓인 대화·카드·생성 이력을 비운다.
--
-- 컨버터가 프리픽스 없는 값을 평문으로 읽어주므로 기존 행도 조회는 된다. 문제는 그 행들이
-- (1) 평문인 채로 덤프에 남고 (2) 블라인드 인덱스가 비어 있어 검색되지 않는다는 것이다. 즉
-- 그냥 두면 "보호도 안 되고 검색도 안 되는 행"이 영구히 남는다.
--
-- 백필로 살리려면 지금은 없는 "메시지 본문 UPDATE" 경로를 새로 열어야 한다. 본문·카드 요약은
-- 생성 후 바뀌지 않는 값이라 수정 경로가 아예 없는데, 백필 때문에 뚫으면 그 구멍이 작업이 끝난
-- 뒤에도 남는다. 실사용자 배포 전이라 V27 과 같은 이유로 정리로 대응한다 — 확인 시점
-- (2026-08-19) prod 의 사용자 메시지는 14건·평균 5자 미만이었고 삭제 합의를 받았다.
--
-- 참조 관계는 V27 과 같다. 네 테이블 안에서만 서로를 가리키므로(cards.conversation_id,
-- generation_log.conversation_id, messages 의 두 참조) 함께 비우면 끊어진 참조가 남지 않는다.
-- 회원·소셜 계정·디바이스 토큰·프롬프트 설정은 남긴다(로그인과 프롬프트가 그대로 유지된다).
-- 새로 만든 환경에서는 대상 행이 없어 아무 일도 하지 않는다.

DELETE FROM messages;
DELETE FROM cards;
DELETE FROM conversations;
DELETE FROM generation_log;
